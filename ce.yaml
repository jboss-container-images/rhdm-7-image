builds:
  # Product builds
  - name: "rhdm-controller-dev"
    descriptor: "controller/image.yaml"
    product: true
    overrides:
      - "controller/dev-overrides.yaml"
  - name: "rhdm-decisioncentral-dev"
    descriptor: "decisioncentral/image.yaml"
    product: true
    overrides:
      - "decisioncentral/dev-overrides.yaml"
  - name: "rhdm-decisioncentral-indexing-dev"
    descriptor: "decisioncentral-indexing/image.yaml"
    product: true
    overrides:
      - "decisioncentral-indexing/dev-overrides.yaml"
  - name: "rhdm-kieserver-dev"
    descriptor: "kieserver/image.yaml"
    product: true
    overrides:
      - "kieserver/dev-overrides.yaml"
  - name: "rhdm-optaweb-employee-rostering-dev"
    descriptor: "optaweb-employee-rostering/image.yaml"
    product: true
    overrides:
      - "optaweb-employee-rostering/dev-overrides.yaml"
